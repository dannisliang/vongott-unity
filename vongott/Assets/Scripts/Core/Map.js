#pragma strict

class Map {
	var mapFile;
	var spawnPoints : List.<Vector3> = new List.<Vector3>();
	
	function Map ( /* map file */) {
		/* parse map data */
		
		/*
		for ( var s in spawnpoints ) {
			spawnPoints.Insert ( s.chapter, s.position );
		}
		*/
	}	
}